# GhostFix Real User Project Fixtures

These lightweight projects intentionally fail at runtime so GhostFix can be
tested against small, realistic developer workflows without external services.

They are local-only fixtures. Do not add credentials, network calls, or
auto-fix expansion here.
